name: Auto Build Flatpak from BrowserOS Deb

on:
  schedule:
    - cron: '0 3 * * *'  # Daily at 3 AM UTC
  workflow_dispatch:

jobs:
  build-flatpak:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get latest BrowserOS release
        id: browseros
        uses: actions/github-script@v7
        with:
          script: |
            const { data: release } = await github.rest.repos.getLatestRelease({
              owner: 'browseros-ai',
              repo: 'BrowserOS'
            });
            const deb = release.assets.find(a => a.name.endsWith('.deb'));
            if (!deb) core.setFailed('No .deb found in latest release');
            core.setOutput('version', release.tag_name.replace(/^v/, ''));
            core.setOutput('deb_url', deb.browser_download_url);
            core.setOutput('deb_name', deb.name);

      - name: Skip if already released
        id: check
        uses: actions/github-script@v7
        with:
          script: |
            const tag = `browseros-${{ steps.browseros.outputs.version }}`;
            const { data } = await github.rest.repos.listReleases({
              owner: context.repo.owner,
              repo: context.repo.repo
            });
            core.setOutput('skip', data.some(r => r.tag_name === tag) ? 'true' : 'false');

      - name: Download .deb
        if: steps.check.outputs.skip != 'true'
        run: |
          curl -L -o deb.deb "${{ steps.browseros.outputs.deb_url }}"

      - name: Install build tools
        if: steps.check.outputs.skip != 'true'
        run: |
          sudo apt update
          sudo apt install -y flatpak flatpak-builder elfutils dpkg

      - name: Install Freedesktop runtime & SDK (user installation)
        if: steps.check.outputs.skip != 'true'
        run: |
          flatpak remote-add --user --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
          flatpak install --user -y flathub \
            org.freedesktop.Platform//25.08 \
            org.freedesktop.Sdk//25.08

      - name: Create Flatpak manifest
        if: steps.check.outputs.skip != 'true'
        run: |
          cat > ai.browseros.BrowserOS.json << 'EOF'
          {
            "id": "ai.browseros.BrowserOS",
            "runtime": "org.freedesktop.Platform",
            "runtime-version": "25.08",
            "sdk": "org.freedesktop.Sdk",
            "command": "BrowserOS",
            "finish-args": [
              "--share=ipc",
              "--share=network",
              "--socket=fallback-x11",
              "--socket=wayland",
              "--device=dri",
              "--socket=pulseaudio",
              "--filesystem=home",
              "--filesystem=host",
              "--talk-name=org.freedesktop.secrets",
              "--device=shm"
            ],
            "modules": [
              {
                "name": "browseros",
                "buildsystem": "simple",
                "build-commands": [
                  "dpkg-deb -x deb.deb .",
                  "mkdir -p /app",
                  "mv opt/BrowserOS/* /app/",
                  "chmod +x /app/BrowserOS",
                  "mkdir -p /app/bin",
                  "ln -s ../BrowserOS /app/bin/BrowserOS"
                ],
                "sources": [
                  { "type": "file", "path": "deb.deb" }
                ]
              }
            ]
          }
          EOF

      - name: Build Flatpak bundle
        if: steps.check.outputs.skip != 'true'
        run: |
          flatpak-builder --user --install --force-clean \
            --install-deps-from=flathub \
            build-dir ai.browseros.BrowserOS.json

          flatpak build-bundle build-dir \
            BrowserOS-${{ steps.browseros.outputs.version }}.flatpak \
            ai.browseros.BrowserOS

      - name: Create Release
        if: steps.check.outputs.skip != 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: browseros-${{ steps.browseros.outputs.version }}
          name: BrowserOS ${{ steps.browseros.outputs.version }} Flatpak
          body: |
            Auto-converted Flatpak from official BrowserOS v${{ steps.browseros.outputs.version }} .deb
          files: BrowserOS-${{ steps.browseros.outputs.version }}.flatpak
